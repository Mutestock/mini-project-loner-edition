version: "3"

services:
  rest:
    build:
      context: "./rest"
      dockerfile: dockerfile
    container_name: deno-deploy
    ports:
      - "15000:15000"
    networks:
      - app-network

  #grpc:
#
  #soap:

  #postgres:



networks:
  app-network:
    driver: "bridge"